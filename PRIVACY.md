# 隐私与数据说明

## 数据存储

番茄记录、任务信息、设置、AI 对话和计划默认保存在设备本地 Preferences 中。应用未实现账户系统，也不会主动把完整本地历史上传到开发者服务器。

## AI 请求

使用 DeepSeek 智能体时，应用会发送用户输入、当前计划以及用于规划的专注摘要，包括今日和近 7 天的专注时长、完成数量、放弃数量和最近任务摘要。发送这些信息是生成个性化计划所必需的。

穿戴协同实验室当前只生成明确标注的模拟心率数据。个人基线会保存在设备本地，逐样本模拟数据只在页面运行期间保留。当未来接入真实穿戴设备时，应用应先在本地计算窗口心率中位数、趋势、持续高负荷时间、信号质量和活动干扰；只有用户审阅允许动作与保护规则、确认预填并再次主动发送后，才会把这些聚合摘要加入 AI 请求。逐秒心率、原始传感器序列和完整健康历史不应发送给 AI 服务。

请勿在目标、任务名称、备注或对话中输入身份证号、银行卡号、密码等敏感信息。

## API Key

- DeepSeek API Key 仅保存在当前组件运行内存中。
- API Key 不写入 Preferences、不进入对话历史，也不包含在仓库和 HAP 的配置文件中。
- 退出应用后需要重新输入。
- 面向多用户发布时，建议改用带鉴权、限流和日志脱敏的服务端代理。

## 用户控制

用户可以在历史页面编辑或删除番茄记录，可以新建 AI 对话并在不同历史会话间切换。卸载应用会清除其本地数据；删除前请自行确认是否需要保留记录。

心率适应功能遵循用户确认原则：无效数据不会触发建议，运动干扰不会直接改变计划，两分钟恢复不会消耗原专注时间，应用也不会依据心率自动调用 AI、完成或删除任务。该功能不用于医学诊断。

## 网络边界

客户端直连只用于个人使用和演示。代理地址默认要求 HTTPS，本机调试仅额外允许 `localhost` 和 `127.0.0.1`。项目不在仓库中提供或保存任何真实服务密钥。
