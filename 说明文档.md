# 番茄钟（Pomodoro Timer）应用开发说明文档

## 一、应用概述

### 1.1 项目简介

番茄钟是一款基于 **HarmonyOS ArkTS** 开发的效率工具应用，采用经典的番茄工作法（Pomodoro Technique）理念，帮助用户高效管理专注时间与休息时间。应用支持自定义工作时长、休息时长、每日目标，提供提示音与白噪音辅助专注，并支持深色/浅色主题一键切换。当用户达成每日目标时，会触发专属庆祝动画与音效。

### 1.2 开发平台与工具

| 项目 | 说明 |
|------|------|
| 操作系统 | HarmonyOS 6.0.2（API 22） |
| 开发框架 | ArkUI（声明式 UI）+ ArkTS |
| 应用模型 | Stage 模型 |
| 构建工具 | DevEco Studio |
| 编程语言 | ArkTS（TypeScript 超集） |
| 模拟器 | DevEco Studio 内置模拟器（Phone 形态） |

### 1.3 界面概览

应用包含 **4 个主界面**，通过底部 Tab 导航切换：

| Tab | 界面 | 说明 |
|-----|------|------|
| 计时 | Timer Tab | 核心计时页，含环形进度、今日统计、控制按钮 |
| 设置 | Settings | 参数配置页，所有计时参数与偏好设置 |
| 统计 | Statistics | 数据可视化页，历史完成番茄的柱状图展示 |
| 成就 | Achievements | 成就中心，12 枚徽章分 4 类展示解锁进度 |

---

## 二、功能清单与技术实现

### 2.1 核心计时引擎

**2.1.1 倒计时机制**

计时器采用 `setInterval` 每秒滴答（tick），每 tick 执行以下逻辑：

```
remainingSeconds -= 1 → updateProgress() → 判断是否归零
```

- **工作模式**下倒计时从设定工作时长（默认 25 分钟）到 0
- **休息模式**下倒计时从设定休息时长（默认 5 分钟）到 0
- 进度值 `progressValue = remainingSeconds / totalSeconds * 100`，驱动环形进度条动画

**2.1.2 自动开始下一轮**

用户可在设置中开启「休息结束自动开始工作」开关。开启后，每次计时结束会自动在 2 秒后启动下一轮计时，无需手动点击开始。设置项通过 `PreferencesHelper.saveAutoStart()` 持久化存储。

**2.1.3 跳过 / 提前结束**

计时期间用户可通过 → 按钮 **跳过当前阶段**（工作→休息 或 休息→工作）。工作时提前结束会记录已完成的工作时长并保存 session；休息时跳过会记录已休息时长。

### 2.2 音效系统

**2.2.1 提示音播放**

提示音采用 HarmonyOS **AVPlayer 状态机**驱动，支持 5 种可选音效：

| 音效名称 | 文件名 | 特点 |
|----------|--------|------|
| 钟铃 | bell.wav | 经典铃声，悠长清晰 |
| 风铃 | chime.wav | 清脆悦耳，柔和提醒 |
| 短促 | beep.wav | 简洁短促，不打扰 |
| 双响 | double_beep.wav | 两声提示，明确清晰 |
| 叮咚 | ding.wav | 清脆叮咚，轻快活泼 |

**AVPlayer 状态机流程**：

```
idle → (fdSrc赋值) → initialized → (prepare) → prepared → (play) → playing
                                                                      ↓
                                                                    completed
                                                                      ↓
                                                         (stop) → stopped
```

关键技术决策：使用 `context.resourceManager.getRawFd()` 获取 rawfile 文件描述符，通过 `player.fdSrc` 赋值（而非 `player.url`），这是 HarmonyOS 本地音频播放的正确方式。每次播放前必须先 `await player.reset()` 和 `await player.release()` 清理上一个播放器实例，否则会导致状态冲突。

**2.2.2 白噪音（白噪音辅助）**

独立于提示音的循环播放系统，仅在**专注模式**计时期间播放，休息时自动停止。提供 4 种选项：

| 选项 | 说明 |
|------|------|
| 关闭 | 不播放任何白噪音 |
| 雨声 | 模拟下雨的自然白噪声（30s 循环） |
| 咖啡馆 | 模拟咖啡馆的低频背景音（30s 循环） |
| 森林 | 柔和的自然音 + 鸟鸣（30s 循环） |

白噪音通过独立的 `AmbientHelper` 模块管理，拥有自己的 AVPlayer 实例，与提示音播放器完全隔离，因此 **白噪音与提示音可同时播放**（例如计时结束时的提示音不会被打断）。

循环实现：AVPlayer 播放到 `completed` 状态后 → `stop()` → `stopped` → `reset()` → 重新设置 `fdSrc` → 形成闭环循环。

**2.2.3 庆祝音效**

当用户达成每日目标时（今日完成番茄数 ≥ 每日目标数），触发 `celebration.wav` 庆祝音效，与视觉动画同步播放。

### 2.3 通知系统

**2.3.1 权限申请**

应用启动时自动检测通知权限状态，若未授权则通过 `notificationManager.requestEnableNotification()` 弹出系统授权弹窗。HarmonyOS 的通知权限独立于通用敏感权限体系，不通过 `module.json5` 的 `requestPermissions` 声明。

**2.3.2 持续通知（Ongoing Notification）**

计时开始后，通知栏显示实时进度通知：

- 标题：「番茄钟计时中」
- 内容：「工作中/休息中 MM:SS」
- 每 **1 秒**刷新一次，实现通知栏倒计时实时跳动
- 计时停止或页面销毁时自动取消

该通知的 `id` 为 `NOTIFICATION_ONGOING_ID (10)`，每次 `publish` 使用相同 id 进行覆盖更新。关键发现：不设置 `notificationSlotType` 属性时，更新通知不会触发系统通知音效，这是确保每秒刷新不产生噪音的核心配置。

**2.3.3 计时结束通知**

工作或休息结束时，发布常规通知 + 触发振动（`vibrator.startVibration`，500ms），配合 SoundHelper 播放提示音，形成三重提醒保障（视觉通知 + 声音 + 振动）。

**2.3.4 通知频道**

结束通知使用 `notificationSlotType: SlotType.SOCIAL_COMMUNICATION`，确保在系统通知中心以醒目方式呈现。

### 2.4 主题切换

利用 HarmonyOS **资源限定符系统**实现深色/浅色主题一键切换：

- `resources/base/color.json` — 基础颜色定义
- `resources/dark/color.json` — 深色主题颜色覆盖
- `resources/light/color.json` — 浅色主题颜色覆盖

所有 `.ets` 文件通过 `$r('app.color.xxx')` 引用颜色资源。切换时调用 `applicationContext.setColorMode()`：

```
COLOR_MODE_DARK  → 深色模式（背景 #000000）
COLOR_MODE_LIGHT → 浅色模式（背景 #F2F2F7）
```

入口 `EntryAbility.onCreate` 设置为 `COLOR_MODE_NOT_SET`（跟随系统），`Index.aboutToAppear` 加载用户偏好后调用 `setColorMode` 覆盖。用户选择通过 `PreferencesHelper.saveTheme()` 持久化 boolean 值。

### 2.5 庆祝动画

当日目标达成时触发全屏庆祝效果：

- **触发条件**：`prevTodayCount < dailyTarget && newTodayCount >= dailyTarget`
- **视觉**：番茄图标缩放动画（0.3 → 1，EaseOut 曲线）+「今日目标达成！」文字 +「太棒了，继续加油！」副标题
- **音频**：同步播放 `celebration.wav`
- **交互**：点击任意位置关闭动画，或 4 秒后自动消失

### 2.6 数据统计

统计页经过优化，提供三个维度的时间聚合数据，顶部概览卡片与柱状图联动：

**2.6.1 概览卡片**

切换 Tab 时自动重新计算并展示：

| 指标 | 说明 |
|------|------|
| 总专注 | 所选时间范围内的累计专注时长 |
| 日均 | 有记录的活跃天数的平均专注时长 |
| 活跃天数 | 有至少一次专注记录的天数 |
| 单日最佳 | 时间范围内的单日最高专注时长 |

**2.6.2 Tab 维度与聚合方式**

| Tab | API 方法 | 数据来源 | 呈现 |
|-----|----------|----------|------|
| 日 | `getStats(ctx, 7)` | 最近 7 天，每天独立 | 7 根柱 "M/D" |
| 周 | `getWeeklyStats(ctx, 4)` | 最近 4 周，按周汇总 | 4 根柱 "M/D-M/D" |
| 月 | `getMonthlyStats(ctx, 6)` | 最近 6 个月，按月汇总 | 6 根柱 "M月" |

周的汇总逻辑：以本周一为基准，往前推 4 周，每日 session 记录按所属周累加，标签格式为"起始日期-结束日期"。月的汇总按 `YYYY-MM` 前缀匹配累加，标签格式为"M月"。

**2.6.3 柱状图视觉优化**

- **渐变柱体**：有数据柱体使用 `linearGradient` 从上到下由 `primary_red` → `#FFCDD2` 渐变过渡，无数据柱体保持浅灰色占位
- **圆角顶部**：`borderRadius(8)` 使柱顶更柔和
- **今天高亮**：当前日期标签显示"今天"并以 `primary_red` 加粗标记
- **弹入动画**：柱体 500ms EaseOut 动画
- **空状态引导**：无任何记录时显示 📊 图标 +「暂无数据」+「开始计时后，专注数据将在这里展示」提示文案

### 2.9 成就中心

成就中心展示 12 枚成就徽章，分 4 类呈现，基于用户历史专注数据自动计算解锁状态：

**2.9.1 徽章分类与触发条件**

| 分类 | 颜色 | 徽章 | 解锁条件 |
|------|------|------|----------|
| 里程（milestone） | 玫红 | 初次番茄 / 番茄新秀 / 番茄达人 / 番茄大师 | 累计完成 1/10/50/100 个番茄 |
| 毅力（streak） | 天蓝 | 初显毅力 / 坚持一周 / 全勤周 | 连续 3/7 天专注；任意一周 7 天全勤 |
| 冠军（champion） | 绿色 | 单日王者 / 周冠军 / 月冠军 | 单日 8 个番茄；一周超 10 小时；一月超 40 小时 |
| 时间（time） | 黄色 | 早起鸟儿 / 夜猫子 | 早上 6-8 点 / 晚上 22-24 点完成过番茄 |

**2.9.2 数据计算引擎**

`AchievementHelper.compute()` 从 Session 历史中提取以下维度：
- 总工作 session 数 → 里程类徽章
- 每日最高番茄数 → 单日王者
- 历史最长连续天数 + 当前连续天数 → 毅力类徽章
- 是否存在连续 7 天记录 → 全勤周
- 周/月总时长最大值 → 周冠军/月冠军
- 按时段筛选完成时间 → 早起鸟儿/夜猫子

周聚合以周一为基准计算 ISO 周范围，月聚合按 `YYYY-MM` 前缀累加。

**2.9.3 UI 布局**

- 顶部进度概览卡片：已解锁数量 + 待解锁数量 + 进度条
- 四组徽章网格（Grid 3 列），每组有分类标签（彩色圆点 + 分类名）
- 已解锁徽章显示彩色背景 + 白色图标 + 加粗名称；未解锁显示灰色背景 + 低透明度
- 页面可见时自动刷新成就状态（`@Watch` 监听 Tab 激活）

### 2.7 后台恢复

应用从后台恢复时，通过 `recoverFromBackground()` 方法计算已流逝时间（`elapsed = Date.now() - savedAt`），自动调整剩余秒数。如果后台时间已超过倒计时剩余时间，自动触发计时完成逻辑。计时器状态通过 `PreferencesHelper.saveTimerState()` 实时保存。

### 2.8 本地数据持久化

所有数据通过 HarmonyOS `@kit.ArkData` 的 **Preferences API** 存储，数据结构如下：

| Key | 类型 | 默认值 | 用途 |
|-----|------|--------|------|
| workDuration | number | 1500 | 工作时长（秒） |
| breakDuration | number | 300 | 休息时长（秒） |
| dailyTarget | number | 8 | 每日番茄目标数 |
| autoStart | boolean | false | 自动开始下一轮 |
| theme | boolean | false | 深色/浅色主题 |
| sound | string | 'bell' | 当前提示音 |
| ambient | string | '' | 当前白噪音 |
| sessionHistory | JSON 数组 | [] | 历史 session 记录 |
| timerState | JSON 对象 | null | 计时器运行状态 |

Session 记录结构：
```typescript
{
  date: string;       // "YYYY-MM-DD"
  workSeconds: number; // 实际工作的秒数
  completedAt: number; // 完成时间戳
  type: 'work' | 'break';
}
```

每日统计通过 `getStats(ctx, days)` 方法计算，先按日期建立空 Map，再将 session 记录的 `workSeconds` 累加到对应日期，最终按时间正序输出 `DailyStats[]`。

---

## 三、模块化代码结构

### 3.1 项目目录结构

```
entry/src/main/ets/
├── common/
│   └── Constants.ets          # 全局常量定义
├── components/
│   ├── CircularProgress.ets   # 环形进度组件
│   ├── TimerControls.ets      # 计时控制按钮组
│   └── CelebrationOverlay.ets # 庆祝动画覆盖层
├── utils/
│   ├── PreferencesHelper.ets  # 数据持久化封装
│   ├── SoundHelper.ets        # 提示音播放器
│   ├── AmbientHelper.ets      # 白噪音播放器
│   ├── AchievementHelper.ets  # 成就计算引擎
│   └── NotificationHelper.ets # 通知与振动管理
├── views/
│   ├── Settings.ets           # 设置页 + SoundOption/AmbientOption 子组件
│   ├── Statistics.ets         # 统计页
│   └── Achievements.ets       # 成就中心页
├── pages/
│   └── Index.ets              # 唯一 @Entry 主页面
└── entryability/
    └── EntryAbility.ets       # 应用入口 Ability
```

### 3.2 架构设计模式

**单页面 Tabs 架构**：`Index.ets` 是唯一的 `@Entry` 组件，所有功能通过 `Tabs` 组件组织在同一页面内，使用 `TabsController` 编程式控制 Tab 切换。

**单向数据流**：状态由 Index 持有（`@State`），通过 `@Prop` 向下传递给子组件，子组件通过回调函数向上通知事件。这避免了跨组件状态同步问题。

```
Index（@State：isRunning, remainingSeconds, isWorkMode, ...）
  ├── CircularProgress（@Prop：progress, remainingSeconds, totalSeconds, isWorkMode）
  ├── TimerControls（@Prop：isRunning, isWorkMode；回调：onStartPause, onReset, onSkip）
  ├── Settings（@Prop：isDarkMode, soundName, ambientName；回调：onThemeChange, onSoundChange, onAmbientChange, onSaved）
  ├── Statistics（独立加载，不接收 props）
  ├── Achievements（@Prop active：监听 Tab 切回时刷新）
  └── CelebrationOverlay（@Prop show；回调 onDismiss）
```

### 3.3 各模块职责

**common/Constants.ets** — 全局常量集中管理。包含默认值（`DEFAULT_WORK_SECONDS`、`DEFAULT_BREAK_SECONDS`、`DEFAULT_DAILY_TARGET`）、Preferences 存储键名（`PREF_KEY_*`）、通知 ID（`NOTIFICATION_*`）、振动时长（`VIBRATION_DURATION_MS`）、通知刷新间隔（`NOTIFICATION_UPDATE_INTERVAL`）。

**utils/PreferencesHelper.ets**（约 340 行）— 数据持久化层。封装 `@kit.ArkData.preferences` API，所有方法均为 `static async`。包含：工作/休息时长、每日目标、主题、音效、白噪音、Session 历史（JSON 数组）、计时器状态（JSON 对象）的存取方法；`getTodaySessions()`、`getTodayWorkSeconds()`、`getStats()`（日统计）、`getWeeklyStats()`（周聚合，以周一为基准按周汇总）、`getMonthlyStats()`（月聚合，按月前缀匹配汇总）等统计方法。

**utils/SoundHelper.ets** — 提示音播放器。使用模块级变量 `player`（而非静态属性）来符合 ArkTS 的 `arkts-no-standalone-this` 规则。通过 `getRawFd()` 加载 rawfile 下的 WAV 文件，基于 AVPlayer 状态机事件驱动（`stateChange` 回调）完成播放全流程。

**utils/AmbientHelper.ets** — 白噪音播放器。独立的 AVPlayer 实例 + 循环播放逻辑。模块级函数 `loopPlay()` 在 `stopped` 状态时重新设置 `fdSrc` 实现无缝循环。`activeAmbient` 变量防止并发切换时的竞态条件。

**components/CircularProgress.ets** — 环形进度组件。双层 Progress 环（轨道环 15% 透明度 + 前景环实色），前景环带 1200ms EaseOut 弹簧动画。居中显示等宽数字倒计时（MM:SS）与模式标签（专注/休息）。

**components/TimerControls.ets** — 三个控制按钮的按钮组：重置（↺）、开始/暂停（动态图标切换）、跳过（→）。每个按钮通过 `onTouch` 事件实现按下缩放（0.9×）的触感反馈。

**components/CelebrationOverlay.ets** — 庆祝动画覆盖层组件。接收 `@Prop show` 和 `onDismiss` 回调，展示番茄图标缩放动画 + 目标达成文案，点击或 4 秒后自动消失。从 Index 拆分出来，减少主页面 UI 代码。

**utils/NotificationHelper.ets** — 通知与振动管理模块。封装通知权限请求、持续通知刷新（每秒更新倒计时）、计时结束通知 + 振动、取消通知等静态方法，Index 无需直接依赖 `@kit.NotificationKit` 和 `@kit.SensorServiceKit`。

**views/Settings.ets** — 设置页。包含 7 个卡片：工作时长、休息时长、每日目标（均含 SVG 图标加减按钮）、自动开始开关（Toggle 组件）、主题切换（浅色/深色二选一按钮组）、提示音选择（5 个 SoundOption 胶囊按钮）、白噪音选择（4 个 AmbientOption 胶囊按钮）。整个页面包裹在 `Scroll` 组件中支持纵向滚动。

**views/Statistics.ets**（约 260 行）— 优化版统计页。顶部概览卡片（总专注/日均/活跃天数/单日最佳，随 Tab 联动）+ 3 Tab（日/周/月）+ 横向滚动渐变柱状图 + 空状态引导。日使用 `getStats()` 按天展示，周使用 `getWeeklyStats()` 按周聚合（标签"M/D-M/D"），月使用 `getMonthlyStats()` 按月聚合（标签"M月"）。柱体使用 `linearGradient` 红→粉渐变，今天日期"今天"高亮标记，EaseOut 弹入动画。

### 3.4 资源文件

| 目录 | 内容 |
|------|------|
| `resources/base/element/` | color.json、float.json、string.json |
| `resources/dark/element/` | color.json（深色覆盖） |
| `resources/light/element/` | color.json（浅色覆盖） |
| `resources/base/media/` | SVG 图标（tomato、pause、start、rest、focus、pomodoro、settings、statistics、plus、minus） |
| `resources/rawfile/` | WAV 音频（bell、chime、beep、double_beep、ding、celebration、ambient_rain、ambient_cafe、ambient_forest） |

---

## 四、页面跳转逻辑

### 4.1 应用启动流程

```
EntryAbility.onCreate()
  ↓ 设置 colorMode 为 COLOR_MODE_NOT_SET（跟随系统）
  ↓ 加载 pages/Index
  ↓
Index.aboutToAppear()
  ↓ 1. 请求通知权限（notificationManager.requestEnableNotification）
  ↓ 2. 加载用户偏好（主题、提示音、自动开始、白噪音）
  ↓ 3. 应用主题（setColorMode）
  ↓ 4. 加载设置参数（工作时长、休息时长、每日目标）
  ↓ 5. 初始化计时状态
  ↓ 6. 加载今日统计数据
  ↓ 7. 尝试恢复后台计时状态
```

### 4.2 Tab 导航

应用使用底部 3 Tab 导航（`Tabs` + `TabContent`）：

```
[计时] [设置] [统计]
  ↑      ↑      ↑
  Tab   Tab    Tab
  0      1      2
```

- 切换到「计时」Tab（索引 0）时触发 `onNavigateToTimer()`，重新加载设置参数
- 从「设置」保存并返回时，通过 `tabsController.changeIndex(0)` 跳回计时页
- 「统计」Tab 通过 `onPageShow()` 生命周期钩子在每次可见时刷新数据

### 4.3 计时状态机

```
初始态（未开始）
  ↓ 点击开始
运行态（倒计时中）
  ↓ 倒计时归零 / 提前结束 / 跳过
完成态
  ↓ 自动开始 (if enabled) → 2s 后
  ↓ 回到初始态（下一轮）
```

状态流转细节：
- **工作和休息的切换**：`switchToWork()` / `switchToBreak()` 重置 `totalSeconds` 和 `remainingSeconds`
- **暂停**：清除 `timerId`，保持 `remainingSeconds` 不变
- **重置**：`remainingSeconds = totalSeconds`，不改变模式
- **后台恢复**：从 Preferences 读取上次保存的状态，计算已流逝时间

### 4.4 页面生命周期

```
aboutToAppear（首次加载）
  → onPageShow（每次可见）
  → onPageHide（隐藏时保存状态）
  → aboutToDisappear（销毁前清理通知和计时器）
```

---

## 五、设备能力调用

### 5.1 通知（Notification Kit）

- **模块**：`@kit.NotificationKit` → `notificationManager`
- **权限**：运行时通过 `requestEnableNotification()` 申请
- **功能**：持续通知 + 计时结束通知

### 5.2 振动（Sensor Service Kit）

- **模块**：`@kit.SensorServiceKit` → `vibrator`
- **触发时机**：计时结束时 500ms 振动
- **说明**：模拟器可能不支持振动硬件，但权限声明和 API 调用已正确配置

### 5.3 音频播放（Media Kit）

- **模块**：`@kit.MediaKit` → `media.AVPlayer`
- **文件格式**：PCM 16-bit mono WAV
- **加载方式**：`context.resourceManager.getRawFd()` → `fdSrc`

### 5.4 本地存储（ArkData）

- **模块**：`@kit.ArkData` → `preferences`
- **存储位置**：应用沙箱内的 preferences 文件
- **数据格式**：key-value + JSON 序列化

### 5.5 主题（ConfigurationConstant）

- **模块**：`@kit.AbilityKit` → `ConfigurationConstant.ColorMode`
- **切换方式**：`applicationContext.setColorMode()`

---

## 六、ArkTS 关键约束与解决方案

| 约束 | 问题 | 解决方案 |
|------|------|----------|
| `arkts-no-standalone-this` | 静态方法不能使用 `this` | 使用模块级 `let` 变量替代静态属性 |
| 不能使用 `any`/`unknown` | 类型不安全 | 所有变量显式声明具体类型 |
| catch 不能省略参数 | 编译错误 | 使用 `catch { }`（无括号） |
| `$r()` 不能拼接字符串 | 编译错误 | 使用 if/else 或条件表达式替代 |
| 不能用相对 import | 编译错误 | 使用 `../utils/xxx` 形式 |
| AVPlayer 不能用 `url` | 音频无声 | 使用 `fdSrc` + `getRawFd()` |

---

## 七、测试与验证

### 7.1 功能测试要点

| 测试场景 | 预期结果 |
|----------|----------|
| 开启计时 | 环形进度条倒计时，通知栏显示实时进度 |
| 计时结束 | 播放提示音 + 振动 + 通知 + 自动切换模式 |
| 开启自动开始 | 计时结束 2s 后自动开始下一轮 |
| 选择白噪音 | 专注时播放循环音效，休息时停止 |
| 达成每日目标 | 触发全屏庆祝动画 + 庆祝音效 |
| 切换深色/浅色 | 全局颜色即时生效 |
| 查看统计 | 柱状图按日/周/月正确显示 |
| 后台恢复 | 返回前台后倒计时正确调整 |
| 设置页滚动 | 7 个卡片可纵向滚动查看 |

### 7.2 模拟器限制说明

- **振动功能**：模拟器不支持实际振动硬件，但 `vibrator.startVibration()` 调用已正确配置，真机可正常运行
- **通知显示**：模拟器通知栏可正常显示，下拉通知栏可查看实时计时进度
- **音频播放**：模拟器支持 WAV 音频播放

---

## 八、代码统计

| 类别 | 文件数 | 代码行数 |
|------|--------|----------|
| pages（主页面） | 1 | 653 |
| views（视图） | 3 | 949 |
| components（组件） | 3 | 195 |
| utils（工具类） | 5 | 695 |
| common（常量） | 1 | 24 |
| entryability（入口） | 1 | 47 |
| 资源文件 | 20+ | — |
| **总计** | **14** | **2563** |

（注：行数不含 JSON 配置文件和 WAV/PNG/SVG 资源文件）

---

## 九、总结

番茄钟应用综合运用了 HarmonyOS ArkUI 声明式开发框架的以下核心技术能力：

1. **UI 开发能力**：自定义环形进度组件、Bento 风格卡片布局、庆祝动画覆盖层、柱状图数据可视化、主题一键切换、页面纵向滚动
2. **设备能力调用**：通知栏实时倒计时刷新、计时结束声音与振动提醒、音频播放（AVPlayer 状态机）
3. **交互能力**：按钮触感缩放反馈、Tab 切换、开关 Toggle、胶囊按钮选择器
4. **模块化架构**：common → utils → components → views → pages 分层设计，单向数据流
5. **本地数据存储**：Preferences API 持久化全部用户设置与历史记录，支持后台状态恢复

应用在模拟器中可完整运行，所有核心功能均已实现并验证通过。
