# 番茄钟（Pomodoro Timer）应用开发说明文档

## 一、项目概述

### 1.1 项目简介

番茄钟是一款基于 **HarmonyOS ArkTS** 开发的效率工具应用，采用经典番茄工作法（Pomodoro Technique）理念，帮助用户在“专注工作”和“短暂休息”之间形成节奏化的时间管理习惯。

本项目不仅实现了基础倒计时功能，还围绕课程项目中常见的考核重点进行了扩展：

- 使用 **ArkUI 声明式 UI** 构建多页面、多组件的移动端界面
- 使用 **Notification Kit** 实现通知栏实时倒计时
- 使用 **Sensor Service Kit** 实现计时结束振动提醒
- 使用 **Media Kit AVPlayer** 实现提示音、庆祝音效和白噪音播放
- 使用 **ArkData Preferences** 持久化用户设置、历史记录与计时状态
- 通过统计页、成就中心、主题切换、庆祝动画等功能提升完整度和展示效果

### 1.2 开发环境

| 项目 | 说明 |
|------|------|
| 操作系统 | HarmonyOS 6.0.2（API 22） |
| 开发框架 | ArkUI 声明式 UI + ArkTS |
| 应用模型 | Stage 模型 |
| 开发工具 | DevEco Studio |
| 构建运行 | DevEco Studio IDE / Previewer / 模拟器 |
| 编程语言 | ArkTS（TypeScript 超集） |
| 数据存储 | ArkData Preferences |

### 1.3 页面结构

应用采用底部 Tab 导航，共包含 **4 个主界面**：

| Tab | 页面 | 主要功能 |
|-----|------|----------|
| 计时 | Timer | 环形倒计时、今日目标进度、开始/暂停/重置/跳过 |
| 设置 | Settings | 工作时长、休息时长、每日目标、主题、提示音、白噪音、自动开始 |
| 统计 | Statistics | 日/周/月专注数据统计、概览卡片、渐变柱状图 |
| 成就 | Achievements | 12 枚成就徽章、4 类分类展示、自动解锁进度 |

---

## 二、项目特色与亮点

### 2.1 UI 展示能力

本项目重点体现了移动端 UI 开发能力，界面不是简单表单堆叠，而是结合了卡片布局、动画、数据可视化和主题适配：

1. **Bento 风格信息卡片**  
   计时页将今日完成进度、专注时长、休息时长等信息以卡片形式展示，层级清晰。

2. **自定义环形进度组件**  
   使用双层 `ProgressType.Ring` 展示倒计时进度，中心显示 MM:SS 倒计时文本和当前模式。

3. **底部四 Tab 导航**  
   使用 `Tabs` + `TabContent` 组织计时、设置、统计、成就四个页面，符合移动端常见导航方式。

4. **数据可视化柱状图**  
   统计页按日、周、月展示专注时长，使用渐变柱体、圆角、动画和空状态引导提升视觉效果。

5. **成就徽章网格**  
   成就页使用 `Grid` 将 12 枚徽章按“里程、毅力、冠军、时间”分组展示，已解锁与未解锁状态有明显视觉区分。

6. **深色 / 浅色主题切换**  
   基于 HarmonyOS 资源限定符系统和 `setColorMode()` 实现主题切换，颜色资源统一管理。

7. **庆祝动画覆盖层**  
   达成每日目标时显示全屏庆祝动画，包含番茄图标缩放、提示文案与庆祝音效，增强反馈感。

### 2.2 HarmonyOS 设备能力调用

项目不仅停留在 UI 层，还调用了多个 HarmonyOS 系统能力：

| 能力 | 使用模块 | 项目中的体现 |
|------|----------|--------------|
| 通知 | `@kit.NotificationKit` | 通知栏显示实时倒计时，计时结束发送提醒 |
| 振动 | `@kit.SensorServiceKit` | 工作/休息结束时触发 500ms 振动 |
| 音频播放 | `@kit.MediaKit` | 提示音、庆祝音效、白噪音播放 |
| 本地存储 | `@kit.ArkData` | 持久化设置项、历史 session、后台计时状态 |
| 主题模式 | `@kit.AbilityKit` | 深色/浅色模式切换 |

### 2.3 工程化与可维护性

项目采用分层结构，将主页面、视图、组件、工具类和常量分离：

```text
common       全局常量
utils        数据、通知、音频、成就等工具封装
components   可复用 UI 组件
views        设置、统计、成就等页面级视图
pages        应用入口与 Tab 容器
```

后期将原本较臃肿的 `Index.ets` 进一步拆分，将通知逻辑抽离到 `NotificationHelper.ets`，将庆祝动画抽离为 `CelebrationOverlay.ets`，降低主页面复杂度，体现了模块化重构能力。

### 2.4 创新功能

相比基础番茄钟，本项目额外增加了以下增强功能：

- **通知栏实时倒计时**：用户切到后台后仍能在通知栏看到剩余时间
- **白噪音辅助专注**：支持雨声、咖啡馆、森林三类循环白噪音
- **成就中心**：根据历史专注数据自动解锁 12 枚徽章
- **统计可视化**：按日、周、月聚合专注时间，展示柱状图
- **目标达成庆祝**：每日目标完成时触发动画和音效

---

## 三、功能模块与实现说明

### 3.1 核心计时模块

计时模块位于 `Index.ets`，使用 `setInterval` 每秒更新一次剩余时间：

```text
remainingSeconds -= 1
  ↓
updateProgress()
  ↓
更新环形进度、通知栏倒计时
  ↓
remainingSeconds <= 0 时处理阶段完成
```

主要状态包括：

| 状态 | 说明 |
|------|------|
| `remainingSeconds` | 当前阶段剩余秒数 |
| `totalSeconds` | 当前阶段总秒数 |
| `isWorkMode` | 是否处于专注模式 |
| `isRunning` | 计时器是否运行中 |
| `progressValue` | 环形进度条显示值 |
| `todayCount` | 今日已完成番茄数量 |

计时结束后，应用会根据当前模式自动切换：

```text
工作结束 → 记录专注 session → 播放提示音 → 发送通知/振动 → 切换到休息
休息结束 → 记录休息 session → 播放提示音 → 发送通知/振动 → 切换到工作
```

如果用户开启“自动开始下一轮”，系统会在 2 秒后自动启动下一阶段。

### 3.2 设置模块

设置页提供用户可调节的计时参数和个性化偏好：

| 设置项 | 类型 | 说明 |
|--------|------|------|
| 工作时长 | 数值调节 | 默认 25 分钟 |
| 休息时长 | 数值调节 | 默认 5 分钟 |
| 每日目标 | 数值调节 | 默认 8 个番茄 |
| 自动开始下一轮 | Toggle | 休息结束后自动开始工作 |
| 主题模式 | 二选一 | 深色 / 浅色 |
| 提示音 | 胶囊按钮 | 5 种 WAV 提示音 |
| 白噪音 | 胶囊按钮 | 关闭 / 雨声 / 咖啡馆 / 森林 |

这些设置都通过 `PreferencesHelper` 写入本地 Preferences，应用重启后仍能保留用户偏好。

### 3.3 通知与振动模块

通知相关逻辑封装在 `NotificationHelper.ets` 中，避免主页面直接依赖通知 SDK。

主要方法：

| 方法 | 作用 |
|------|------|
| `requestPermission()` | 请求通知权限 |
| `publishOngoing()` | 发布/更新通知栏实时倒计时 |
| `sendEndNotification()` | 发送计时结束通知并触发振动 |
| `cancelOngoing()` | 取消持续通知 |

持续通知的关键点是：**每秒使用相同通知 ID 覆盖更新，并且不设置 `notificationSlotType`**。这样可以让通知栏倒计时实时刷新，同时避免每次刷新都播放系统通知音。

### 3.4 音效与白噪音模块

音频播放使用 HarmonyOS `media.AVPlayer`，本地音频文件存放在 `resources/rawfile/` 中。

提示音由 `SoundHelper.ets` 管理，白噪音由 `AmbientHelper.ets` 管理，两者使用独立播放器实例，因此提示音和白噪音互不干扰。

AVPlayer 的状态机流程如下：

```text
createAVPlayer()
  ↓
fdSrc 赋值
  ↓
initialized → prepare()
  ↓
prepared → play()
  ↓
completed → stop()
  ↓
stopped → reset/release
```

白噪音播放完成后会重新设置 `fdSrc` 并再次播放，实现循环效果。

### 3.5 统计模块

统计页通过 `PreferencesHelper` 读取历史 session 记录，并进行不同维度的聚合：

| 维度 | 方法 | 展示方式 |
|------|------|----------|
| 日 | `getStats(ctx, 7)` | 最近 7 天每日专注时长 |
| 周 | `getWeeklyStats(ctx, 4)` | 最近 4 周每周总专注时长 |
| 月 | `getMonthlyStats(ctx, 6)` | 最近 6 个月每月总专注时长 |

统计页顶部还会显示概览指标：

- 总专注时长
- 日均专注时长
- 活跃天数
- 单日最佳

柱状图使用渐变色、圆角和动画，空数据时显示引导文案，避免页面空白。

### 3.6 成就模块

成就系统由 `AchievementHelper.ets` 负责计算，`Achievements.ets` 负责展示。

共设计 12 枚成就，分为 4 类：

| 分类 | 颜色 | 成就示例 | 触发条件 |
|------|------|----------|----------|
| 里程 | 玫红 | 初次番茄、番茄新秀、番茄达人、番茄大师 | 累计完成 1/10/50/100 个番茄 |
| 毅力 | 天蓝 | 初显毅力、坚持一周、全勤周 | 连续专注或任意 7 天全勤 |
| 冠军 | 绿色 | 单日王者、周冠军、月冠军 | 单日/周/月专注强度达标 |
| 时间 | 黄色 | 早起鸟儿、夜猫子 | 在指定时间段完成番茄 |

成就页顶部展示已解锁数量、待解锁数量和进度条；下方使用 3 列 Grid 展示徽章。已解锁徽章使用彩色圆形背景和白色图标，未解锁徽章使用灰色低透明度样式。

### 3.7 后台恢复模块

应用隐藏到后台时，会将当前计时状态写入 Preferences：

```typescript
{
  remainingSeconds: number,
  totalSeconds: number,
  isWorkMode: boolean,
  isRunning: boolean,
  completedSessions: number,
  savedAt: number
}
```

返回前台后，`recoverFromBackground()` 会根据 `savedAt` 计算后台经过的时间，并自动修正剩余秒数。如果后台经过时间已经超过倒计时剩余时间，则直接触发计时完成逻辑。

---

## 四、UI 开发能力体现

### 4.1 ArkUI 声明式布局

项目使用 ArkUI 声明式方式构建界面，主要使用以下布局组件：

| 组件 | 使用场景 |
|------|----------|
| `Column` / `Row` | 页面主体、卡片、按钮组排列 |
| `Stack` | 计时页中庆祝动画覆盖层叠加 |
| `Tabs` / `TabContent` | 四个主页面导航 |
| `Scroll` | 设置页、统计页、成就页滚动内容 |
| `Grid` / `GridItem` | 成就徽章三列网格布局 |
| `Progress` | 环形倒计时进度 |
| `Divider` | 概览卡片分隔线 |

### 4.2 组件化设计

项目将可复用 UI 拆分为独立组件：

| 组件 | 职责 |
|------|------|
| `CircularProgress` | 环形倒计时显示 |
| `TimerControls` | 开始/暂停/重置/跳过按钮组 |
| `CelebrationOverlay` | 每日目标达成庆祝动画 |

这种拆分使主页面只负责状态和流程控制，UI 细节由组件内部维护，提高了代码可读性。

### 4.3 视觉与交互细节

项目包含多种 UI 细节处理：

- 按钮按下缩放反馈
- 环形进度 EaseOut 动画
- 柱状图高度变化动画
- 今日目标进度胶囊条
- 已解锁/未解锁成就状态区分
- 深浅色主题资源自动适配
- 空数据状态提示
- 全屏庆祝覆盖层

这些设计使应用不仅具备功能性，也具备较好的用户体验和展示效果。

---

## 五、HarmonyOS 能力调用说明

### 5.1 Notification Kit

应用启动时请求通知权限，计时运行时发布持续通知。通知内容包含当前模式和剩余时间，例如：

```text
番茄钟计时中
工作中 24:31
```

计时结束时，应用发布单独的结束通知：

```text
番茄钟
工作时间结束，休息一下吧！
```

### 5.2 Sensor Service Kit

工作或休息结束时调用 `vibrator.startVibration()` 触发 500ms 振动。模拟器可能无法真实振动，但 API 调用流程已实现，真机环境可触发硬件反馈。

### 5.3 Media Kit

项目使用 `media.AVPlayer` 播放本地 WAV 文件，包含：

- 计时结束提示音
- 每日目标达成庆祝音效
- 专注阶段循环白噪音

本地文件通过 `context.resourceManager.getRawFd()` 获取文件描述符，再赋值给 `player.fdSrc` 播放。

### 5.4 ArkData Preferences

本地数据全部通过 Preferences 保存，包括：

| Key | 默认值 | 用途 |
|-----|--------|------|
| `workDuration` | 1500 | 工作时长 |
| `breakDuration` | 300 | 休息时长 |
| `dailyTarget` | 8 | 每日目标 |
| `autoStart` | false | 自动开始下一轮 |
| `theme` | false | 主题偏好 |
| `sound` | bell | 提示音选择 |
| `ambient` | 空字符串 | 白噪音选择 |
| `sessionHistory` | [] | 历史记录 |
| `timerState` | null | 后台恢复状态 |

### 5.5 主题能力

通过 `applicationContext.setColorMode()` 设置应用颜色模式：

```text
COLOR_MODE_DARK   深色模式
COLOR_MODE_LIGHT  浅色模式
```

颜色统一在 `resources/base/element/color.json`、`resources/dark/element/color.json`、`resources/light/element/color.json` 中维护，页面通过 `$r('app.color.xxx')` 引用。

---

## 六、工程结构与模块职责

### 6.1 目录结构

```text
entry/src/main/ets/
├── common/
│   └── Constants.ets
├── components/
│   ├── CircularProgress.ets
│   ├── TimerControls.ets
│   └── CelebrationOverlay.ets
├── utils/
│   ├── PreferencesHelper.ets
│   ├── SoundHelper.ets
│   ├── AmbientHelper.ets
│   ├── AchievementHelper.ets
│   └── NotificationHelper.ets
├── views/
│   ├── Settings.ets
│   ├── Statistics.ets
│   └── Achievements.ets
├── pages/
│   └── Index.ets
└── entryability/
    └── EntryAbility.ets
```

### 6.2 数据流设计

项目采用单页面 Tabs 架构，`Index.ets` 是唯一 `@Entry` 页面，负责维护全局状态并向下传递属性：

```text
Index
  ├── CircularProgress
  ├── TimerControls
  ├── Settings
  ├── Statistics
  ├── Achievements
  └── CelebrationOverlay
```

数据流遵循“父组件持有状态、子组件通过回调通知父组件”的模式：

- `Index` 使用 `@State` 保存运行状态
- 子组件使用 `@Prop` 接收状态
- 设置页通过回调通知 Index 更新主题、音效、白噪音等偏好
- 成就页通过 `@Prop active` 感知 Tab 激活并刷新数据

### 6.3 各模块职责

| 模块 | 职责 |
|------|------|
| `Index.ets` | Tab 容器、计时状态机、页面状态协调 |
| `Settings.ets` | 用户设置页面 |
| `Statistics.ets` | 历史数据统计与可视化 |
| `Achievements.ets` | 成就中心展示 |
| `PreferencesHelper.ets` | Preferences 数据读写和统计聚合 |
| `NotificationHelper.ets` | 通知权限、持续通知、结束通知、振动 |
| `SoundHelper.ets` | 提示音和庆祝音效播放 |
| `AmbientHelper.ets` | 白噪音循环播放 |
| `AchievementHelper.ets` | 成就解锁计算 |
| `CircularProgress.ets` | 环形倒计时组件 |
| `TimerControls.ets` | 计时控制按钮组件 |
| `CelebrationOverlay.ets` | 庆祝动画覆盖层 |

---

## 七、关键技术难点与解决方案

### 7.1 通知栏每秒刷新但不能每秒响铃

**问题**：持续通知每秒更新，如果使用普通通知频道配置，系统可能在每次 `publish()` 时播放通知音。

**解决方案**：持续通知使用固定 `NOTIFICATION_ONGOING_ID` 覆盖更新，并且不设置 `notificationSlotType`。这样通知内容会刷新，但不会每秒触发系统提示音。计时结束通知则单独使用通知频道，保证结束提醒足够明显。

### 7.2 AVPlayer 状态机播放本地音频

**问题**：HarmonyOS AVPlayer 不能简单顺序调用 `prepare()` 和 `play()`，必须按照状态机事件驱动。

**解决方案**：在 `stateChange` 回调中处理状态转换：

```text
initialized → prepare()
prepared → play()
completed → stop()
stopped → release/reset
```

本地音频不使用 `url`，而是通过 `getRawFd()` 获取 rawfile 文件描述符并赋值给 `fdSrc`。

### 7.3 周/月统计聚合

**问题**：统计页不能只展示最近若干天，还需要按周、按月汇总，更符合用户查看长期专注趋势的习惯。

**解决方案**：

- 周统计：以本周一为基准，向前取 4 周，每周累加 7 天 session 时长
- 月统计：按 `YYYY-MM` 前缀匹配历史记录，汇总最近 6 个月

### 7.4 成就解锁计算

**问题**：成就不仅依赖总完成次数，还依赖连续天数、单日最高、周/月时长和完成时间段。

**解决方案**：`AchievementHelper.compute()` 统一读取历史记录，构建日期 Map、周 Map、月 Map，并计算：

- 累计番茄数
- 每日最高番茄数
- 当前连续天数和历史最长连续天数
- 任意连续 7 天记录
- 周/月最大专注时长
- 早晨与夜晚完成记录

### 7.5 后台计时恢复

**问题**：应用进入后台后，前台定时器可能停止，返回前台时需要恢复真实剩余时间。

**解决方案**：隐藏页面时保存 `savedAt` 时间戳，恢复时计算 `Date.now() - savedAt` 得到经过时间，再修正 `remainingSeconds`。如果经过时间超过剩余时间，则直接触发阶段完成。

---

## 八、测试与验证

### 8.1 功能验证表

| 测试场景 | 操作 | 预期结果 |
|----------|------|----------|
| 开始计时 | 点击开始按钮 | 环形进度开始下降，通知栏出现倒计时 |
| 暂停计时 | 运行中点击暂停 | 倒计时停止，剩余时间保持不变 |
| 重置计时 | 点击重置按钮 | 当前阶段时间恢复为初始值 |
| 跳过阶段 | 运行中点击跳过 | 工作/休息阶段切换 |
| 计时结束 | 等待倒计时归零 | 播放提示音、发送通知、触发振动并切换模式 |
| 自动开始 | 开启自动开始后完成阶段 | 2 秒后自动开始下一阶段 |
| 白噪音 | 设置雨声/咖啡馆/森林并开始专注 | 专注时播放，休息时停止 |
| 主题切换 | 设置页切换深色/浅色 | 页面颜色即时变化 |
| 统计页 | 完成若干番茄后查看统计 | 日/周/月柱状图显示正确 |
| 成就页 | 达成成就条件后切换到成就页 | 对应徽章显示为已解锁状态 |
| 后台恢复 | 计时中切到后台再返回 | 剩余时间按真实经过时间修正 |
| 每日目标 | 今日完成数达到目标 | 显示庆祝动画并播放庆祝音效 |

### 8.2 模拟器限制说明

- 模拟器支持通知栏显示和音频播放
- 模拟器可能不支持真实振动硬件反馈，但振动 API 调用已实现
- 项目需要通过 DevEco Studio 构建运行，未配置独立 CLI 构建命令

---

## 九、代码统计

| 类别 | 文件数 | 代码行数 |
|------|--------|----------|
| pages（主页面） | 1 | 653 |
| views（页面视图） | 3 | 949 |
| components（组件） | 3 | 195 |
| utils（工具类） | 5 | 695 |
| common（常量） | 1 | 24 |
| entryability（入口） | 1 | 47 |
| **总计** | **14** | **2563** |

说明：统计不包含 JSON 配置文件、SVG 图标、WAV 音频等资源文件。

---

## 十、课程评分点对应关系

| 评分关注点 | 项目体现 |
|------------|----------|
| UI 开发能力 | 四 Tab 页面、卡片式布局、环形进度、柱状图、Grid 徽章、动画、主题切换 |
| HarmonyOS 能力调用 | 通知、振动、音频播放、本地存储、颜色模式切换 |
| ArkTS 使用能力 | `@State`、`@Prop`、`@Watch`、`@Builder`、异步方法、类型约束处理 |
| 数据处理能力 | session 历史记录、日/周/月聚合、成就解锁计算、后台时间恢复 |
| 工程化能力 | common/utils/components/views/pages 分层，Helper 工具类封装，Index 拆分重构 |
| 用户体验 | 白噪音、提示音、通知栏实时倒计时、每日目标进度、庆祝动画、空状态提示 |
| 完整度 | 设置、计时、统计、成就、主题、通知、音频、持久化均已形成闭环 |

---

## 十一、总结

本项目从基础番茄钟出发，逐步扩展为一个包含计时、设置、统计、成就、通知、音频、主题和本地存储的完整 HarmonyOS 应用。项目实现过程中综合使用了 ArkUI 声明式 UI、ArkTS 状态管理、HarmonyOS 多项系统能力以及模块化工程结构。

从课程展示角度看，本项目的重点不只是“能倒计时”，而是通过多页面 UI、数据可视化、设备能力调用和工程化拆分，体现了较完整的 HarmonyOS 应用开发能力。应用在 DevEco Studio 模拟器中可完整运行，核心功能均已实现并经过手动验证。
