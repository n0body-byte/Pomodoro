# 更新记录

本项目采用“主版本.次版本.修订号”的版本格式。

## 未发布 - 复赛优化

### 新增

- AI 返回计划先进入待确认提案，展示新增、修改、删除和番茄总量变化，支持逐项采用、全部应用或拒绝。
- 每轮有效专注后提供轻量复盘面板，记录专注度、精力、打断原因和实际产出；反馈会同步进入历史与 Agent 上下文。
- 计时页新增沉浸模式，隐藏底部导航与次要信息，只保留当前任务、倒计时和阶段控制。
- HarmonyOS 跨设备接续原型，支持手机、平板与 2in1 设备迁移当前计时、任务、Agent 状态和最近 50 条专注记录。
- 接续数据增加版本、大小与格式校验，通过唯一传输标识和记录键保证重复接续不会重复累计历史。
- 接续目标端持久化最近传输时间，拒绝重复或更旧的快照，避免旧设备状态把较新的恢复进度回滚。
- 目标设备按绝对截止时间恢复运行中计时，并通过全局修订信号刷新计时页与 AI 页面。
- Agent 执行结果核验：达到预计番茄数后要求记录可检查交付、部分进度或具体阻塞，避免把投入时间直接等同于任务成功。
- 部分完成或阻塞会把临时完成的步骤恢复为未完成，DeepSeek 复盘提示携带用户证据并要求拆小、重估或解除阻塞。
- DeepSeek 调用审计按会话记录生成、复盘和质量修复的模型、耗时、Token 与结果，可直接判断是否收到远端响应。
- DeepSeek 对话级 Token 预算支持 1 万、3 万和 6 万三档，达到 80% 时预警，达到上限后停止新调用及自动修复。
- 模型返回空内容、截断或格式异常时，只要响应包含 usage，仍保留真实 Token 数据；审计记录不保存提示词和 API Key。
- AI 目标完整度守门：输入时实时识别“学习英语、完善项目”等空泛目标，在调用 API 前追问本轮产出、数量或范围。
- AI 计划硬性质量门：不允许容量超限、缺少完成标准、步骤重复、动作空泛或单步超过 4 个番茄的计划进入执行。
- DeepSeek 首版计划未通过本地质检时自动修复一次；第二次仍未通过即停止调用，并保留原计划、展示具体原因与 Token 消耗。
- AI 请求失败时可根据目标、可用时间和番茄参数生成本地应急计划，保证弱网或服务波动时仍可完成演示和继续执行。
- 本地应急计划使用独立来源标识和醒目标签，明确说明未调用 AI，不混淆模型生成结果。
- 网络恢复后可一键把本地应急计划交给 AI，在不增加总番茄数的前提下优化步骤、交付物和风险预案。
- 截止时间从提示词升级为本地强约束，支持绝对时间、今晚/明天/周几和相对时长；格式无效或已经过期时不会调用 AI。
- 设置页新增“穿戴协同实验室”，无需真机即可用虚拟手表同步查看并控制当前计时。
- 新增可替换的 `HeartRateProvider`、四种确定性心率场景和本地适应规则，为后续真实手表接入保留稳定边界。
- 心率摘要进入 Agent 前过滤逐秒数据、无效样本与运动干扰，并由用户确认后才允许发送和调整计划。
- 模拟心率升级为逐样本滑动窗口，增加置信度、个人基线校准与本地持久化；演示模式明确标注 `×30` 时间加速。
- 持续高负荷可进入独立两分钟恢复，冻结原专注倒计时，恢复后由用户选择继续、缩短或按实际时长结束。
- 穿戴建议升级为结构化 Agent 调整请求，展示允许动作和保护规则；确认只预填请求，不会自动调用 AI。
- 历史记录新增任务、项目、标签、产出和打断原因搜索，以及专注、休息、完成状态和复盘状态筛选。
- 历史专注任务支持“一键重做”，安全恢复任务上下文与估算番茄数并返回计时页。
- 数据页新增行动洞察，综合高效时段、估时准确度、专注反馈与主要打断生成下一轮建议。

### 改进

- AI 页面使用 ArkUI 栅格响应窗口宽度，手机保持单列操作顺序，平板与 2in1 在 720vp 以上自动切换为 5:7 双栏。
- AI 会话持久化计划来源，历史恢复后仍能正确区分联网智能体计划与本地应急计划。
- AI 计划步骤应用到计时器时记录独立跟踪起点，旧的同名任务记录不再导致新步骤误完成；计时页同时标注任务来自 AI 计划。
- AI 任务与所属会话建立关联，多番茄步骤的部分完成量会反馈到计划进度、剩余耗时和容量风险。
- AI 计划步骤新增“立即开始专注”，一次完成任务绑定、来源追踪、切回专注阶段和启动倒计时；已运行或已消耗的阶段会拒绝切换。
- 新增不消耗 Token 的本地计划质检，检查容量、重复步骤、完成标准、安排原因和交付闭环，并支持一键交给 DeepSeek 修正。
- DeepSeek 设置页新增连接诊断，不发送目标或对话内容即可检查密钥、余额可用状态和所选模型，并区分常见失败原因。
- AI 步骤完成状态按来源会话精确回写；即使计划已归档也不会误更新其他对话，手动同名任务不再误推进 AI 计划。
- 计时页在 AI 步骤达到预计专注量后显示待核验卡片，引导先确认真实交付再安排下一步。
- DeepSeek 确实修改已有计划时自动保存单级版本快照，用户可不调用 AI 恢复上版；快照随当前及历史会话持久化。
- 本地应急计划会把过大的核心阶段继续拆分，确保每个执行步骤不超过 4 个番茄。
- AI 页面增加“目标 / 计划 / 执行 / 复盘”阶段指示，减少首次使用时的操作迷失。
- 本地单元测试扩展至 73 个核心场景，并完成 ArkTS 编译与未签名 HAP 打包验证。

## 1.1.0 - 2026-07-22

### 新增

- DeepSeek 多轮执行智能体，可结合专注历史生成、调整和复盘计划。
- 本地计划健康度引擎，计算加权进度、剩余番茄、预计耗时、时间缓冲和可行度。
- 计划超载预警与一键 AI 压缩计划。
- AI 历史会话归档、恢复及步骤完成进度显示。
- 智能体长短期记忆摘要，可查看近 7 天节奏、本轮主目标、计划进度与下一步。
- 目标偏移检测，以及按主目标校准计划的建议动作。
- 真实计时与步骤估时偏差超过 25% 时的主动重排建议。
- 默认关闭、可明确配置的完成步骤自动复盘。
- 版本化未签名 HAP、完整 README、项目说明 PDF 和真实模拟器截图。

### 改进

- 底部导航由 6 个入口精简为“计时 / AI 计划 / 数据 / 设置”4 个主入口，统计、历史和成就整合到数据页内切换。
- 将常驻“智能体行动中心”改为情境化“下一步建议”，仅在需要干预时出现，并说明原因和执行结果。
- AI 请求失败后保留原始行动并支持一键重试，意外异常不会停留在“分析中”。
- 已有计划支持离线继续执行，仅生成、复盘和重排需要调用 AI 服务。
- 可用时间和截止条件随 AI 会话保存，恢复历史对话后继续使用原约束。
- 提前结束、跳过阶段和自然完成的统计口径保持一致。
- 计时页累计专注与休息时长在记录变化后立即刷新。
- 本地单元测试扩展至 23 个核心场景。

## 1.0.0 - 2026-07-21

- 完成番茄计时、任务上下文、统计、历史、成就、主题、声音和长休息基础能力。
